create project: "cmake .. -G "Unix Makefiles" -DCMAKE_C_COMPILER=gcc -DCMAKE_CXX_COMPILER=g++"      # MUST BE RAN FROM TERMINAL IN: {PROJECT_FOLDER}/build/

build project: "cmake --build ."                                                                    # MUST BE RAN FROM TERMINAL IN: {PROJECT_FOLDER}/build/